# Error Handling

Standardized error types and error handling utilities for the backend application.

## Components

- **`errors.go`** - Custom error types, HTTP status code mapping, and error message formatting

Provides error classification, logging, and tracking infrastructure used across all service and handler layers.