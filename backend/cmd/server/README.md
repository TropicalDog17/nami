# Server Application

Main HTTP server application entry point and initialization.

## Components

- **`main.go`** - Server initialization with routing, middleware, and dependency injection

Handles HTTP server configuration, application lifecycle, graceful shutdown, and connects all service layers for the transaction tracking API.