# React Context

Global state management using React Context providers for shared application data.

## Components

- **`AppContext.tsx`** - Global application state, user data, and actions
- **`BackendStatusContext.tsx`** - Backend connectivity and status monitoring

Manages shared application state, provides data to components throughout the tree, and handles side effects, data fetching, and state persistence.